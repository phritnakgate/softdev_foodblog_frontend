name: softdev_foodblog_frontend
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.4.4

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  image_picker: ^0.8.6
  permission_handler: ^10.0.0
  http: ^1.2.2
  iconify_flutter: ^0.0.5
  page_transition: ^2.1.0
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/images/burger.png
    - lib/images/buritto.png
